name: fire_storage_upload
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^1.0.5
  cupertino_icons: ^1.0.2
  firebase_core: ^1.0.3
  firebase_storage: ^8.0.3
  image_picker: ^0.7.4
  transparent_image: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
